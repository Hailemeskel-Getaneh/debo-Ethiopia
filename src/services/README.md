# Services Folder

The API layer of the application. Contains Axios instances and service files that match your database tables.

- **Includes**: `api.ts`, `auth.service.ts`, `donation.service.ts`, `gallery.service.ts`, etc.
