# Types Folder

Contains shared TypeScript interfaces and types that match your database schema and application domain.

- **Included**: `user.ts`, `donation.ts`, `contact.ts`, `gallery.ts`, `achievement.ts`.
